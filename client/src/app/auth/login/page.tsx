import LoginPage from "./LoginPage";

const page = () => {
  return (
    <div className="flex items-center justify-center h-[93vh] mx-2 md:mx-0">
      <LoginPage />
    </div>
  );
};

export default page;
