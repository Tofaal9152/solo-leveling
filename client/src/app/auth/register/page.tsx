import RegisterPage from "./RegisterPage";

const page = () => {
  return (
    <div className="flex items-center justify-center h-[93vh]">
      <RegisterPage />
    </div>
  );
};

export default page;
